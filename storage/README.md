# Document storage

Runtime data for the ingestion pipeline. Keep input PDFs in `raw/`; derived chunk data belongs in `processed/`; files that cannot be ingested should be placed in `failed/` for investigation.

Avoid committing sensitive, proprietary, or large source documents.
