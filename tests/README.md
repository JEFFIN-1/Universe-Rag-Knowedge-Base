# Tests

Automated checks for the project. Add focused tests here as ingestion, retrieval, API, and UI behavior become stable. Run the suite with `uv run pytest` after installing any required test dependencies.
