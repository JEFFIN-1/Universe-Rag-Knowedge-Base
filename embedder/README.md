# Embedding model

This package loads the local ONNX version of `Xenova/all-MiniLM-L6-v2`, tokenizes text, mean-pools model output, and L2-normalizes vectors. `download.py` can fetch the model files into `models/Xenova/all-MiniLM-L6-v2/`.
