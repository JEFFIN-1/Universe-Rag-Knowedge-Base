# LLM client

This folder holds the generation provider integration. `client.py` loads `GROQ_API_KEY` from the environment and calls Groq's `llama-3.3-70b-versatile` model with a low temperature for more consistent answers.
