# Monitoring

This package logs question, answer, model, response time, and retrieved-chunk count to PostgreSQL. It expects a local `rag_monitoring` database and a `conversations` table; schema provisioning is not yet included in the repository.
