# API routes

This folder defines FastAPI request and response handling. `routes.py` currently exposes `POST /chat` and accepts a JSON object with a `question` field. The root and health routes are defined in `app/main.py`.
